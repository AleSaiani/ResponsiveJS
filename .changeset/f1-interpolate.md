---
'@responsivejs/core': minor
---

Curve authoring (F1): new `interpolate` module — `linear`/`exponential`/`logarithmic`/`eased` interpolators, `stepped` and `piecewise` multi-segment curves, `cubicBezier` solver, `sample()` bridging back to curve analysis, and `inverse()` queries. Color math gains OKLab: `rgbaToOklab`/`oklabToRgba`, perceptual `mixOklab`, `formatRgb`.
