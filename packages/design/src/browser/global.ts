/**
 * IIFE entry — becomes `window.rjs` in the browser-global bundle.
 * Everything the injectable surface offers: the driver-free oracle
 * (analyzeDOM, scoreDOM, collectors, wire) plus the overlay.
 */

export * from './index.js';
